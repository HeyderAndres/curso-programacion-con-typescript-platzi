//array 
const price: number[] = [1,2,3,4,5];

//tupla
// una tupla es un tipo de dato que nos permmite definir un array con un número fijo de elementos, donde cada elemento puede ser de un tipo distinto.
const user: [string, number] = ['heider', 15];

//destructuring tuple
const [userName, age] = user;

console.log(userName);
console.log(age);

